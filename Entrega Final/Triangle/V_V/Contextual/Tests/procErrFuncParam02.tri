! Error: proc llamado con argumento func para parametro Integer

let
  func F () : Integer ~ 10;
  proc P (var x : Integer) ~ ! P espera paso por variable (referencia) 
    putint (x * 2)
in
  P (F()) ! Error: P solo puede recibir _variable_ no _valor_ resultante de la aplicacion de una _funcion_.
