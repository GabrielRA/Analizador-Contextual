! Prueba del comando de llamada. Debe fallar.
let
   proc p1(a: Integer) ~ begin
                          put('[');
                          putint(a);
                          put(']')
                         end;
   var x: Integer
in
begin
  getint(var x);
  p1(x,'a',2);
  p1()
end